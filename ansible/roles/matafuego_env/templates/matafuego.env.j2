{# Generado por Ansible — no editar a mano en el servidor, se pisa en cada
   corrida del playbook. Cambiá los valores en group_vars/ (o group_vars/vault.yml
   para los secretos) y volvé a correr el playbook.

   Mismo archivo que espera backend-puntoCO2/.github/workflows/deploy-backend.yml
   (ENV_FILE={{ matafuego_env_path }}). PORT es obligatorio: si falta, Next.js
   arranca en el 3000 por defecto pero el healthcheck del workflow prueba
   matafuego_backend_port y el deploy se reporta como fallido. #}
DATABASE_URL=postgresql://{{ matafuego_db_user }}:{{ vault_matafuego_db_password }}@localhost:{{ matafuego_db_host_port }}/{{ matafuego_db_name }}?schema=public
RUNTIME_DATABASE_URL=postgresql://app_user:{{ vault_matafuego_app_user_password }}@localhost:{{ matafuego_db_host_port }}/{{ matafuego_db_name }}?schema=public
AUTH_SECRET={{ vault_matafuego_auth_secret }}
PORT={{ matafuego_backend_port }}
NODE_ENV=production
