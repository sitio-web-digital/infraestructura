{# Generado por Ansible — no editar a mano en el servidor, se pisa en cada
   corrida del playbook. Cambiá los valores en group_vars/ (o group_vars/vault.yml
   para los secretos) y volvé a correr el playbook.

   Mismo archivo que espera campus/.github/workflows/deploy-backend.yml
   (ENV_FILE=/opt/sitioweb/campus.env). ADMIN_EMAIL/ADMIN_PASSWORD solo
   importan en el primer arranque (cuando data/ todavía no tiene ningún
   admin creado, ver README del repo) — arranques siguientes los ignoran. #}
PORT={{ campus_backend_port }}
ADMIN_EMAIL={{ vault_campus_admin_email }}
ADMIN_PASSWORD={{ vault_campus_admin_password }}
